default:
  extensions:
    Laracasts\Behat: ~
      # env_path: .env.behat

    Behat\MinkExtension:
      default_session: laravel
      javascript_session: selenium2
      base_url: http://localhost
      laravel: ~
      selenium2:
        browser: chrome
